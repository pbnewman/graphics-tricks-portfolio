# A Field Guide to Graphics Tricks

A static HTML portfolio/site hosted with GitHub Pages.

## Local preview

Open `index.html` in a browser, or run a small static server from this directory and visit `http://localhost:8000`.
